---
title: "Create agents"
---

# Example Code with Scroll

## Example Code Block

<!-- <div style="max-height: 300px; overflow-y: scroll;">
  <pre>
  
  </code>
  </pre>
</div> -->

```css
     .code-block {
       max-height: 200px; /* Adjust this value as needed */
       overflow-y: auto; 
     }
```

```code-block 

  // This is a sample code that will scroll vertically if it exceeds the height limit.
  function exampleFunction() {
    console.log("Hello, world!");
    // more lines of code...
    for (let i = 0; i < 100; i++) {
      console.log(i);
    }
  }
  exampleFunction();
  // This is a sample code that will scroll vertically if it exceeds the height limit.
  function exampleFunction() {
    console.log("Hello, world!");
    // more lines of code...
    for (let i = 0; i < 100; i++) {
      console.log(i);
    }
  }
  exampleFunction();
  // This is a sample code that will scroll vertically if it exceeds the height limit.
  function exampleFunction() {
    console.log("Hello, world!");
    // more lines of code...
    for (let i = 0; i < 100; i++) {
      console.log(i);
    }
  }
  exampleFunction();
  // This is a sample code that will scroll vertically if it exceeds the height limit.
  function exampleFunction() {
    console.log("Hello, world!");
    // more lines of code...
    for (let i = 0; i < 100; i++) {
      console.log(i);
    }
  }
  exampleFunction();
  ```